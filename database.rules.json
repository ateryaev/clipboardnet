{
  "rules": {
    "clips": {
      ".read": "auth !== null",
      ".indexOn": "createdBy",
      "$clipid": {
        ".write": "auth !== null && ((!data.exists() && newData.child('createdBy').val() === auth.uid ) || (data.child('createdBy').val() === auth.uid && newData.child('createdBy').val() === auth.uid) || (data.child('createdBy').val() === auth.uid && !newData.exists()))"
      }
    },
    "users": {
      "$uid": {
        ".read": "auth !== null && $uid === auth.uid",
        ".write": "auth !== null && $uid === auth.uid"
      }
    }
  }
}